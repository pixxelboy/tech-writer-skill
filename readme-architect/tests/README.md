# Tests

This repository contains an installable agent skill rather than an executable application. The test suite validates the package shape and skill metadata.

Run from the repository root:

```bash
python3 tests/validate_structure.py
```
